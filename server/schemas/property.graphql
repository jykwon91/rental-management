scalar Date
scalar DateTime

type Property {
  id: ID!
  address: String!
  state: String!
  postalCode: String!
  city: String!
  description: String
  createdAt: DateTime
  updatedAt: DateTime
}

input PropertyInput {
  address: String!
  state: String!
  postalCode: String!
  city: String!
  description: String
}
